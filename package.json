{
  "name": "feralmonkey",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "tokens:build": "node tokens/build-css-vars.mjs",
    "tokens:watch": "node --watch tokens/build-css-vars.mjs",
    "prebuild": "npm run tokens:build"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
