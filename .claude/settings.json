{
  "permissions": {
    "allow": [
      "npm run *",
      "node tokens/*",
      "npm install --workspace *"
    ],
    "deny": [
      "rm -rf *",
      "git push *",
      "git reset --hard *",
      "mv packages/*",
      "rm packages/*"
    ]
  }
}
